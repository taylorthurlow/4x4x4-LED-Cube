/*
#ifndef LED_H
#define LED_H

#include "LEDColor.h"

struct led {
	LEDColor color;
	int intensity;
};
typedef struct led led;

void setLedColor(led* inLed, LEDColor newColor);
void setLedIntensity(led* inLed, int newIntensity);

#endif
*/